//Nested for of loop.

let heros = [['ironman', 'Thor', 'spiderman'], ['batman', 'flash', 'superman']];
for (list of heros) {
    // console.log(list);
    for (lists of list) {
        console.log(lists);
    }
}

console.log("dipak")
let i = 1
while (i < 5) {
    console.log(i);
    i++
}